name: Compliance Scrubber
description: Cybersecurity compliance environment.
version: "1.0.0"

observation_space:
  type: text

action_space:
  type: text

endpoints:
  health: /
  reset:  /reset
  step:   /step
  state:  /state

tasks:
  - id: easy
    grader: true
  - id: medium
    grader: true
  - id: hard
    grader: true

scoring:
  reward_range: [0.01, 0.99]
  success_threshold: 0.5